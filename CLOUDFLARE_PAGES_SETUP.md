# 🚀 Cloudflare Pages 部署指南

本指南将帮助你使用 Cloudflare Pages 部署贪吃蛇游戏。

## 📋 部署方式选择

Cloudflare Pages 提供两种部署方式：

### 方式一：通过 Cloudflare Dashboard（推荐，最简单）⭐

这是最简单的方式，无需配置 GitHub Secrets！

#### 步骤：

1. **注册/登录 Cloudflare 账户**
   - 访问：https://dash.cloudflare.com/sign-up
   - 如果没有账户，免费注册一个

2. **创建 Pages 项目**
   - 访问：https://dash.cloudflare.com
   - 点击左侧菜单 **"Workers & Pages"**
   - 点击 **"Create application"** → 选择 **"Pages"** → **"Connect to Git"**

3. **连接 GitHub 仓库**
   - 选择 GitHub 作为 Git 提供商
   - 授权 Cloudflare 访问你的 GitHub 账户
   - 选择仓库：`gantanhaoo/snake-game`
   - 点击 **"Begin setup"**

4. **配置构建设置**
   - **项目名称**：`snake-game`（或自定义）
   - **生产分支**：`master`
   - **构建设置**：
     - **构建命令**：留空（静态网站不需要构建）
     - **输出目录**：`.`（根目录）
   - 点击 **"Save and Deploy"**

5. **等待部署完成**
   - 部署通常需要 1-2 分钟
   - 部署完成后，你会看到：`https://snake-game.pages.dev`
   - 点击链接即可访问！

#### 优势：
- ✅ 无需配置 GitHub Secrets
- ✅ 自动部署（每次 push 代码自动部署）
- ✅ 简单易用，适合新手

---

### 方式二：通过 GitHub Actions（适合高级用户）

如果你想通过 GitHub Actions 自动部署，需要配置以下内容：

#### 步骤：

1. **获取 Cloudflare API Token**
   - 访问：https://dash.cloudflare.com/profile/api-tokens
   - 点击 **"Create Token"**
   - 选择 **"Edit Cloudflare Workers"** 模板
   - 或者自定义权限：
     - **Account** → **Cloudflare Pages** → **Edit**
   - 点击 **"Continue to summary"** → **"Create Token"**
   - **复制 token**（只显示一次，请保存好）

2. **获取 Account ID**
   - 访问：https://dash.cloudflare.com
   - 在右侧边栏找到 **"Account ID"**
   - **复制 Account ID**

3. **配置 GitHub Secrets**
   - 访问：https://github.com/gantanhaoo/snake-game/settings/secrets/actions
   - 点击 **"New repository secret"**
   - 添加以下两个 secrets：
     - Name: `CLOUDFLARE_API_TOKEN`，Value: 你的 API Token
     - Name: `CLOUDFLARE_ACCOUNT_ID`，Value: 你的 Account ID

4. **触发部署**
   - 推送代码到 GitHub
   - GitHub Actions 会自动运行
   - 部署完成后，访问：https://dash.cloudflare.com
   - 在 **"Workers & Pages"** → **"Pages"** 中找到你的项目

---

## ✅ 部署检查清单

- [ ] 注册了 Cloudflare 账户
- [ ] 连接了 GitHub 仓库（方式一）或配置了 GitHub Secrets（方式二）
- [ ] 部署成功
- [ ] 获得了部署链接（如：`https://snake-game.pages.dev`）
- [ ] 在浏览器中测试正常
- [ ] 在微信中测试正常

---

## 🎯 部署后的操作

### 1. 获取部署链接

部署成功后，你会得到一个类似这样的链接：
- `https://snake-game.pages.dev`

### 2. 测试访问

- 在浏览器中打开链接，确认游戏正常运行
- 在微信中打开链接，测试是否被拦截

### 3. 自定义域名（可选）

如果你想使用自己的域名：
1. 在 Cloudflare Pages 项目设置中点击 **"Custom domains"**
2. 添加你的域名
3. 按照提示配置 DNS 记录

---

## 🆘 常见问题

### 问题 1：部署失败

**解决方案：**
- 检查 GitHub 仓库是否正确连接
- 确认分支名称是 `master` 或 `main`
- 查看 Cloudflare Pages 的部署日志

### 问题 2：找不到项目

**解决方案：**
- 访问：https://dash.cloudflare.com
- 进入 **"Workers & Pages"** → **"Pages"**
- 查看所有项目列表

### 问题 3：微信中无法访问

**解决方案：**
- Cloudflare Pages 的 `.pages.dev` 域名通常不会被微信拦截
- 如果被拦截，考虑使用自定义域名

---

## 📝 推荐方式

**建议使用方式一（Dashboard 方式）**，因为：
- 更简单，无需配置 GitHub Secrets
- 自动部署，无需手动触发
- 界面友好，易于管理

---

## 🎉 完成！

部署完成后，你的游戏就可以通过 Cloudflare Pages 的链接访问了！

如果遇到任何问题，可以查看 Cloudflare Pages 的官方文档：https://developers.cloudflare.com/pages/

