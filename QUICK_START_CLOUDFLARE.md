# 🚀 Cloudflare Pages 快速部署指南

## ✨ 自动化配置已完成！

所有必要的配置文件已经为你准备好了。你现在只需要按照以下步骤操作即可完成部署。

---

## 🎯 推荐方式：Cloudflare Dashboard（最简单）

### 步骤 1：运行配置检查脚本

```bash
node setup-cloudflare.js
```

这会验证所有配置文件是否正确。

### 步骤 2：访问 Cloudflare Dashboard

1. **打开浏览器访问**：https://dash.cloudflare.com
   - 如果没有账户，免费注册一个

2. **创建 Pages 项目**：
   - 点击左侧菜单 **"Workers & Pages"**
   - 点击 **"Create application"** → 选择 **"Pages"**
   - 点击 **"Connect to Git"**

3. **连接 GitHub 仓库**：
   - 选择 **GitHub** 作为 Git 提供商
   - 授权 Cloudflare 访问你的 GitHub 账户
   - 选择仓库：`gantanhaoo/snake-game`
   - 点击 **"Begin setup"**

4. **配置构建设置**：
   ```
   项目名称: snake-game
   生产分支: master
   构建设置:
     - 框架预设: None
     - 构建命令: (留空)
     - 输出目录: . (根目录)
   ```
   - 点击 **"Save and Deploy"**

5. **等待部署**：
   - 部署通常需要 1-2 分钟
   - 部署完成后，你会看到：`https://snake-game.pages.dev`

### 步骤 3：测试访问

- 在浏览器中打开部署链接
- 在微信中打开链接测试是否正常

---

## ⚙️ 备选方式：GitHub Actions（需要配置 Secrets）

如果你想使用 GitHub Actions 自动部署，需要：

1. **获取 Cloudflare API Token**：
   - 访问：https://dash.cloudflare.com/profile/api-tokens
   - 创建 Token（选择 "Edit Cloudflare Workers" 模板）

2. **获取 Account ID**：
   - 访问：https://dash.cloudflare.com
   - 在右侧边栏找到 "Account ID"

3. **配置 GitHub Secrets**：
   - 访问：https://github.com/gantanhaoo/snake-game/settings/secrets/actions
   - 添加 `CLOUDFLARE_API_TOKEN` 和 `CLOUDFLARE_ACCOUNT_ID`

4. **推送代码触发部署**

---

## 🔍 验证配置

运行以下命令检查配置是否正确：

```bash
node check-cloudflare-config.js
```

---

## 📖 详细文档

- 完整部署指南：`CLOUDFLARE_PAGES_SETUP.md`
- 自动部署指南：`AUTO_DEPLOY_GUIDE.md`

---

## ✅ 完成检查清单

- [ ] 运行了 `node setup-cloudflare.js` 验证配置
- [ ] 访问了 Cloudflare Dashboard
- [ ] 连接了 GitHub 仓库
- [ ] 配置了构建设置
- [ ] 部署成功
- [ ] 获得了部署链接
- [ ] 在浏览器中测试正常
- [ ] 在微信中测试正常

---

## 🎉 完成！

部署完成后，你的游戏就可以通过 Cloudflare Pages 的链接访问了！

每次推送代码到 GitHub，Cloudflare Pages 会自动重新部署。

